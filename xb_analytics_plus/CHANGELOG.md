## 0.1.0

- Initial release.
- Event model, queueing, sampling, sensitive-field masking.
- Multi-sink fan-out (`console`, `memory`, `store`, custom sink).
- Local persistence (`path_provider` on non-web, `localStorage` on web).
- Export APIs and built-in debug page.
