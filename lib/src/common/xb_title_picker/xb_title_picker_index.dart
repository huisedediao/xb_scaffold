class XBTitlePickerIndex {
  int column;
  int index;

  XBTitlePickerIndex(this.column, this.index);

  @override
  String toString() {
    return 'TitlePickerIndex{column: $column, index: $index}';
  }
}
