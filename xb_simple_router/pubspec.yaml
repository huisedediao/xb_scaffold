name: xb_simple_router
description: Simple injectable router abstraction for Flutter with optional go_router adaptation.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: '>=3.12.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
